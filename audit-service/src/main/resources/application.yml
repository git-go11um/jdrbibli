server:
  port: 8085

spring:
  application:
    name: audit-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/auditdb

logging:
  level:
    root: INFO
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
