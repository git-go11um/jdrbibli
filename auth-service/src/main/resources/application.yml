server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false
    locations: classpath:db/migration

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.springframework : DEBUG

jwt:
  secret: MaSuperClefSecreteTrèsLonguePourHmacSha256DontTuDoisPrendreSoin1234567890
  expiration: 3600000

mail:
  host: smtp.gmail.com
  port: 587
  username: v.haumont@gmail.com
  password: votremotdepasseoumdpapplication
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
